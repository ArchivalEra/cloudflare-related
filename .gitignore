# ==========================================
# 白名单模式 .gitignore：默认忽略一切，只追踪显式放行的文件
# 需要纳入版本控制的新文件，在下方用 "!路径" 放行即可
# ==========================================

# 默认忽略所有内容
*

# 放行目录本身（否则无法追踪子目录内的白名单文件）
!*/

# ---- 放行文件（白名单）----
!.gitignore
!LICENSE

# ---- accelerate 优选研究与实践 ----
!accelerate/**

